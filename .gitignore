# secrets — never commit
.env
*.env
production/.env

# generated output (photos, intro clips, wall.json)
production/output/

# assets are large/generated; only the product assets are force-added (see git add -f)
production/assets/

# python / os / node
__pycache__/
*.pyc
.DS_Store
node_modules/
